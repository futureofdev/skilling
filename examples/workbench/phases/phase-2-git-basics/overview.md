# Git Basics

Files change. Git remembers. This phase takes the folder skills you just practised and adds
a permanent, append-only history: every version of every file, retrievable forever, with a
message saying why it changed.

You will need git installed — run `git --version` in your terminal to check. Most systems
already have it; if yours does not, your tutor can help you install it before the first
lesson.
